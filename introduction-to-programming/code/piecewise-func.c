/*
 * piecewise-func.c
 *
 *  Created on: 20 Nov 2016
 *      Author: fanj
 */

#include<stdio.h>
/*
int main () {
	double x, y;
	int status = 1;
	scanf("%lf", &x);
	if (x >= -1 && x < 0) {
		y = 2 * x + 2;
	} else if (x > 0 && x < 2) {
		y = - x / 2;
	} else if (x >= 2 && x <= 1000) {
		y = 3;
	} else {
		status = 0;
	}
	if (status == 1) {
		int yint = (int) y;
		if (y - yint == 0) printf("%d", yint);
		else {
			printf("%.5f", y);
		}
	} else {
		printf("unsolvable");
	}
	return 0;
}
*/
