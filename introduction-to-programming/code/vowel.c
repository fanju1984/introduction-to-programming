/*
 * vowel.c
 *
 *  Created on: 20 Nov 2016
 *      Author: fanj
 */
/*
#include<stdio.h>

int main() {
	char ch;
	int count = 0;
	int a = 0, e = 0, i = 0, o = 0, u = 0;
	while ((ch = getchar()) != '\n') {
		if (ch == 'a' || ch == 'A') a ++;
		else if (ch == 'e' || ch == 'E') e ++;
		else if (ch == 'i' || ch == 'I') i ++;
		else if (ch == 'o' || ch == 'O') o ++;
		else if (ch == 'u' || ch == 'U') u ++;
		count ++;
	}
	printf("%.1f%%,%.1f%%,%.1f%%,%.1f%%,%.1f%%",
			(float) a * 100 / count,
			(float) e * 100 / count,
			(float) i * 100 / count,
			(float) o * 100 / count,
			(float) u * 100 / count
			);
	return 0;
}
*/
